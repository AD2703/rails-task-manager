<ul>
  <% @tasks.each do |task| %>
    <li><%= check_box('done', task.completed, {checked: task.completed}) %>- <%= link_to task.title, task_path(task) %>
    </li>
  <% end %>
</ul>

<h5 class="btn btn-outline-primary"><%= link_to 'New task', new_task_path %></h5>
